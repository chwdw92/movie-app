import { Button } from "@mui/material";

// have not worked on this part: not the requirement for the interview project
export default function TransitionModal({ children, fetchContentInDetail }) {
  return (
    <div>
        <Button
        variant="contained"
        onClick={() => {
          
        }}
        >
        View in Detail
      </Button>
    </div>
  )
}
